import type { PostMetadata, PostSeries } from './postMetadata';
import type { WorkLink, WorkMetadata, WorkType } from './workMetadata';

export type {
    PostMetadata,
    PostSeries,
    WorkLink,
    WorkMetadata,
    WorkType,
}
